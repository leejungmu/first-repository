name: lee.jungmu
email: lee.jungmu.email@gamil.com
password: 10co2ptJ

git 권한설정
$ git config --global user.name "lee.jungmu"
$ git config --global user.email lee.jungmu.email@gmail.com
$ git config --list

git terminal 
> git status
> git add ****
> git commit -m "comment"
> git push origin master
> git log


※로컬저장소와 원격저장소 연결
// Github 원격저장소와 연결한다.
git remote add origin [자신의 Github 원격저장소 주소] 
// 연결된 원격저장소 확인한다.
git remote -v


※ git clone ▶▶로컬저장소에 원격저장소 생성 
> git pull ▶▶fetch(다운로드만 하고 저장 X) + merge
> git pull --rebase ▶▶fetch + rebase 
                               히스토리가 엇갈려 애매모호한 상황에서 작업을 원격저장소의 최신 상태와 합치기

＊git warning: LF will be replaced by CRLF
→ git config --global core.autocrlf true